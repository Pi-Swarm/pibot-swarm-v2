# 🛡️ طريقة تشغيل Pi-Swarm (نسخة Rust)

## 🚀 التثبيت السريع

### ١. متطلبات النظام
```bash
# تثبيت Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env

# تثبيت Ollama
curl -fsSL https://ollama.com/install.sh | sh
ollama pull qwen2.5:1.5b

# تثبيت nmap (للمسح الشبكي)
sudo apt install nmap
```

### ٢. بناء البرنامج
```bash
# استنساخ المستودع
git clone https://github.com/Pi-Swarm/pibot-swarm-v2.git
cd pibot-swarm-v2

# البناء للاختبار
cargo build

# البناء المُحسّن (للإنتاج)
cargo build --release

# الملف الناتج: target/release/pi
```

---

## 🎮 دليل الاستخدام

### الأوامر الأساسية

```bash
# فحص النظام
./target/release/pi status

# محادثة مع الوكيل
./pi agent "كيف أصلح ثغرة SQL injection؟"

# تدقيق أمني
./pi security audit ./my_code.rs
./pi security audit https://github.com/user/repo

# مسح شبكي
./pi security scan 192.168.1.1

# مهمة أمنية مستقلة
./pi task "ابحث عن ثغرات في المجلد الحالي"

# تشغيل البوابة الدائمة
./pi gateway
```

---

## 🎮 أوامر مفصلة

### ١. التدقيق الأمني (Audit)

```bash
# تدقيق ملف محلي
./pi security audit ./programs/lib.rs

# تدقيق مستودع
./pi security audit https://github.com/KadeshX-Web3/KadeshX

# مراجعة شاملة
./pi task "اقرأ ملف main.rs وابحث عن جميع الثغرات"
```

### ٢. المسح الشبكي (Scan)

```bash
# فحص جهاز واحد
./pi security scan 192.168.1.1

# فحص شبكة
./pi security scan 192.168.1.0/24
```

### ٣. المحادثة مع الذكاء الاصطناعي (Agent)

```bash
# أسئلة أمنية
./pi agent "ما هي أشهر ثغرات Solana؟"
./pi agent "اشرح ثغرة Unchecked Account"
./pi agent "قارن بين أمان Solidity و Rust"

# تخطيط المهام
./pi agent "خطط كيفية تدقيق بروتوكول DeFi"
```

### ٤. المهام المستقلة (Task)

```bash
# مهام مركبة
./pi task "اكلن جميع ملفات Rust وافحصها"
./pi task "ابحث عن ثغرات UncheckedAccount"
./pi task "راجع الكود وأنشئ تقرير أمني"
```

---

## 📱 التحكم عبر Telegram

### الإعداد
```bash
# ١. الحصول على توكن من @BotFather

# ٢. تعيين التوكن
export PI_TELEGRAM_TOKEN="توكنك_الخاص"

# ٣. تشغيل البوابة
./pi telegram
```

### الأوامر على Telegram

| الأمر | الاستخدام | مثال |
|-------|----------|------|
| `/start` | رسالة ترحيب | |
| `/status` | فحص النظام | |
| `/audit` | تدقيق | `/audit https://github.com/...` |
| `/scan` | مسح شبكي | `/scan 192.168.1.1` |
| `/task` | مهمة | `/task ابحث عن ثغرات` |
| `/agent` | محادثة | `/agent ما هي SQLi؟` |
| `/help` | المساعدة | |

---

## 📁 ملفات المشروع

```
pibot-swarm-v2/
├── src/
│   ├── main.rs      ← نقطة الدخول والأوامر
│   ├── agent.rs     ← الوكيل الذكي
│   ├── tools.rs     ← أدوات التدقيق والمسح
│   ├── provider.rs  ← الاتصال بـ Ollama
│   ├── gateway.rs   ← البوابة الدائمة
│   ├── session.rs   ← إدارة الجلسات
│   └── telegram.rs  ← قناة التلغرام
├── Cargo.toml       ← إعدادات Rust
└── README.md        ← التعليمات
```

---

## 💡 نصائح

### ١. التثبيت العالمي
```bash
# نسخ الملف للنظام
sudo cp target/release/pi /usr/local/bin/

# الآن يعمل من أي مكان
pi status
pi agent "مرحباً"
```

### ٢. تطوير جديد
```bash
# تعديل الكود
vim src/main.rs  # أو أي محرر

# اختبار التغييرات
cargo run -- status
cargo run -- agent "اختبار"

# بناء نهائي
cargo build --release
```

### ٣. متغيرات البيئة
```bash
# نموذج الذكاء الاصطناعي
export PI_MODEL="qwen2.5:1.5b"

# منفذ البوابة
export PI_PORT="18789"

# توكن تلغرام
export PI_TELEGRAM_TOKEN="..."
```

---

## 🆚 المقارنة مع OpenClaw

| العمل | OpenClaw | Pi-Swarm |
|-------|----------|----------|
| فحص الحالة | `openclaw status` | `./pi status` |
| محادثة الوكيل | `openclaw agent -m "msg"` | `./pi agent "msg"` |
| تشغيل البوابة | `openclaw gateway` | `./pi gateway` |
| تدقيق أمني | ❌ لا يوجد | `./pi security audit` |
| مسح شبكي | ❌ لا يوجد | `./pi security scan` |
| Telegram | إضافة | مدمج |

---

## 🐛 حل المشاكل

### مشكلة: Ollama غير متصل
```bash
# تشغيل Ollama
ollama serve

# في terminal آخر
./pi status
```

### مشكلة: nmap غير مثبت
```bash
sudo apt install nmap
```

### مشكلة: Cargo غير موجود
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

---

## 📞 الدعم

- **المستودع**: github.com/Pi-Swarm/pibot-swarm-v2
- **التعليمات المفصلة**: انظر README.md (الإنجليزية)
- **البنية التقنية**: انظر RUST_ARCHITECTURE.md

---

**🛡️ سرب ذكاء سيادي. مبني بلغة Rust.**
