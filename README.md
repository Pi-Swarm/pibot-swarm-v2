# 🥧 Pi bot Swarm 2.0

**Autonomous Blue Team Security Operations**

نظام أمني دفاعي متعدد الوكلاء، مستوحى من بنية [Decepticon](https://github.com/PurpleAILAB/Decepticon) ولكن للأغراض الدفاعية (Blue Team).

---

## 📋 المحتويات

- [الميزات](#الميزات)
- [البنية المعمارية](#البنية-المعمارية)
- [التثبيت](#التثبيت)
- [الاستخدام](#الاستخدام)
- [الوكلاء](#الوكلاء)
- [نظام Replay](#نظام-replay)
- [المساهمة](#المساهمة)

---

## ✨ الميزات

- 🤖 **4 وكلاء متخصصون**: استطلاع، تحليل، تخطيط، تقارير
- 🎯 **تنسيق مركزي**: Orchestrator يدير التواصل بين الوكلاء
- 💾 **نظام Replay**: حفظ الجلسات وإعادة تشغيلها
- 🛡️ **فحص حقيقي**: دعم فعلي لفحص الشبكات والمنافذ
- 📊 **تقييم المخاطر**: تحليل تلقائي للمستوى الأمني
- 🔌 **قابل للتوسع**: بنية MCP-style لإضافة أدوات جديدة

---

## 🏗️ البنية المعمارية

```
┌─────────────────────────────────────────────────────────┐
│              🎯 Swarm Orchestrator                       │
│  (Message Bus + Session Management + Coordination)      │
└─────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│ 🕵️ Recon      │  │ 🧠 Analyst    │  │ 🎯 Planner    │
│ Network Scan  │  │ Risk Analysis │  │ Coordination  │
└───────────────┘  └───────────────┘  └───────────────┘
                            │
                            ▼
                   ┌───────────────┐
                   │ 📝 Reporter   │
                   │ Documentation │
                   └───────────────┘
```

---

## 🚀 التثبيت

### المتطلبات

- Python 3.9+
- لا توجد مكتبات خارجية مطلوبة (فقط المكتبات القياسية)

### خطوات التثبيت

```bash
# 1. انتقل إلى مجلد العمل
cd ~/.openclaw/workspace/pibot

# 2. تأكد من وجود الملفات
ls swarm_v2/
# يجب أن ترى:
# __init__.py  agents.py  core.py  main.py  orchestrator.py  tools.py

# 3. (اختياري) أنشئ بيئة افتراضية
python -m venv venv
source venv/bin/activate  # على Linux/Mac
```

---

## 💻 الاستخدام

### الطريقة 1: تشغيل مباشر (Demo)

```bash
cd ~/.openclaw/workspace/pibot
python swarm_v2/main.py
```

سيبدأ هذا مهمة تجريبية على الشبكة `192.168.122.0/24`.

---

### الطريقة 2: وضع تفاعلي

```bash
python swarm_v2/main.py --interactive
```

ستظهر قائمة تفاعلية تتيح لك:
1. بدء مهام جديدة
2. عرض حالة السرب
3. تصدير الجلسات

---

### الطريقة 3: كمبرمج (Python API)

```python
from swarm_v2 import SwarmOrchestrator

# إنشاء المنسق
orchestrator = SwarmOrchestrator()

# بدء مهمة
log_file = orchestrator.start_mission(
    mission_name="فحص أمني",
    target="192.168.122.0/24"
)

# عرض الحالة
status = orchestrator.get_status()
print(status)

# تصدير الجلسة
exported = orchestrator.export_session(log_file.split("_")[1].split(".")[0])
```

---

## 🤖 الوكلاء

### 🕵️ ReconnaissanceAgent (وكيل الاستطلاع)

**المهام:**
- اكتشاف الأجهزة النشطة في الشبكة
- فحص المنافذ المفتوحة
- كشف الخدمات العاملة

**القدرات:**
```python
[
    "network_discovery",
    "port_scanning",
    "service_detection"
]
```

**مثال رسالة:**
```python
AgentMessage(
    sender="Orchestrator",
    recipient="Recon",
    message_type="task",
    content={"task_type": "scan_network", "target": "192.168.122.0/24"}
)
```

---

### 🧠 AnalysisAgent (وكيل التحليل)

**المهام:**
- تحليل نتائج الفحوصات
- تقييم مستوى المخاطر
- كشف الثغرات والأنماط المشبوهة

**القدرات:**
```python
[
    "risk_assessment",
    "threat_detection",
    "anomaly_analysis"
]
```

---

### 🎯 PlannerAgent (وكيل التخطيط)

**المهام:**
- تنسيق العمليات بين الوكلاء
- تحديد الأولويات
- إدارة سير العمل

**القدرات:**
```python
[
    "mission_planning",
    "task_orchestration",
    "workflow_management"
]
```

---

### 📝 ReporterAgent (وكيل التقارير)

**المهام:**
- تجميع النتائج من جميع الوكلاء
- إنشاء تقارير أمنية
- تصدير الجلسات للمشاركة

**القدرات:**
```python
[
    "report_generation",
    "data_aggregation",
    "session_export"
]
```

---

## 💾 نظام Replay

نظام الحفظ وإعادة التشغيل يسمح بـ:

1. **تسجيل الجلسات**: كل رسالة بين الوكلاء تُحفظ
2. **إعادة التشغيل**: يمكن إعادة تشغيل جلسة سابقة
3. **المشاركة المجتمعية**: تصدير الجلسات بصيغة JSON

### مثال: حفظ جلسة

```python
replay = SwarmReplay()
replay.start_session()
replay.log_event("scan_started", {"target": "192.168.122.0/24"})
replay.save_session()  # يحفظ في swarm_logs/session_<id>.json
```

### مثال: تحميل جلسة سابقة

```python
replay = SwarmReplay()
events = replay.load_session("abc123")
for event in events:
    print(f"{event['timestamp']}: {event['event_type']}")
```

---

## 📁 هيكل الملفات

```
pibot/
├── swarm_v2/
│   ├── __init__.py       # نقطة الاستيراد
│   ├── core.py           # الفئات الأساسية
│   ├── agents.py         # الوكلاء الأربعة
│   ├── orchestrator.py   # المنسق المركزي
│   ├── tools.py          # أدوات الفحص الفعلي
│   ├── main.py           # نقطة الدخول
│   └── swarm_logs/       # جلسات محفوظة (يُنشأ تلقائياً)
│
├── swarm_manager.py      # الإصدار القديم (v1)
└── security_report.json  # تقارير سابقة
```

---

## 🔧 التطوير

### إضافة وكيل جديد

```python
# 1. أنشئ فئة جديدة في agents.py
class NewAgent(BaseAgent):
    def __init__(self):
        super().__init__("NewAgent", "Description")
    
    def get_capabilities(self) -> List[str]:
        return ["capability_1", "capability_2"]
    
    def process_message(self, message: AgentMessage):
        # منطق المعالجة
        pass

# 2. سجّله في orchestrator.py
def register_default_agents(self):
    # ... الوكلاء الحاليين
    self.register_agent(NewAgent())
```

### إضافة أداة فحص جديدة

```python
# في tools.py
def my_custom_scan(target: str) -> Dict:
    # كود الفحص
    return result
```

---

## 🤝 المساهمة

نرحب بالمساهمات! يمكنك:

1. إضافة وكلاء جدد متخصصين
2. تطوير أدوات فحص إضافية
3. تحسين نظام التقييم الأمني
4. إضافة دعم TCP/IP متقدم

### كيفية المساهمة

```bash
# 1. Fork المستودع
# 2. أنشئ فرعاً جديداً
git checkout -b feature/new-agent

# 3. قم بالتغييرات
# 4. اختبر изменения
python swarm_v2/main.py

# 5. أرسل Pull Request
```

---

## 📝 الترخيص

مشروع مفتوح المصدر - استخدم بحرية للأغراض التعليمية والدفاعية.

---

## ⚠️ تحذير

استخدم هذا النظام **فقط** على الشبكات التي تملكها أو لديك إذن صريح لفحصها.

> "أفضل دفاع هو هجوم جيد" - لكن دائماً ضمن الحدود القانونية والأخلاقية.

---

## 🎯 مستقبل المشروع

- [ ] دعم MCP Tools مثل Decepticon
- [ ] واجهة Web (Streamlit)
- [ ] تكامل مع Ollama للتحليل الذكي
- [ ] دعم التشفير للجلسات المحفوظة
- [ ] مكتبة ثغرات قابلة للتوسعة

---

**صُنع بـ ❤️ بواسطة Pi bot 🥧**
